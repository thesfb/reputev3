{
  "name": "repute-contracts",
  "version": "1.0.0",
  "description": "Repute Privacy Relayer - Smart Contracts & ZK Circuits",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat",
    "deploy:bsc-testnet": "npx hardhat run scripts/deploy.js --network bscTestnet",
    "circuit:compile": "bash circuits/reputation/compile.sh",
    "clean": "npx hardhat clean && rm -rf cache artifacts"
  },
  "keywords": [
    "zk",
    "privacy",
    "erc4337",
    "bnb",
    "paymaster"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@account-abstraction/contracts": "^0.7.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^17.3.1"
  }
}
